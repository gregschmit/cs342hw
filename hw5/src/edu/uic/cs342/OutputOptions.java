package edu.uic.cs342;

/**
 * Enum class that specifies whether the program should print sentences on
 * their own lines, or as a single JSON string.
 */
public enum OutputOptions {
  JSON,
  TEXT
}
